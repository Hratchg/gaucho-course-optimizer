---
phase: 07-tutorial-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/HomePage.tsx
  - frontend/src/pages/HomePage.test.tsx
autonomous: true
requirements: [TUT-01, TUT-02, TUT-03, TUT-04]

must_haves:
  truths:
    - "Student sees a horizontal stacked bar showing GPA, Quality, Difficulty, and Sentiment segments each at 25%"
    - "Student reads a clear, jargon-free definition and real example value for each of the four scoring factors"
    - "Student follows a three-step usage guide: Search a Course, Compare Professors, Adjust Your Priorities"
    - "Student clicks 'Start Searching' CTA button and navigates to /search"
    - "All section headings, body copy, and example values match the UI-SPEC copywriting contract exactly"
  artifacts:
    - path: "frontend/src/pages/HomePage.tsx"
      provides: "Full tutorial landing page with Hero, Score Breakdown, Factor Cards, Usage Guide, and Final CTA sections"
      min_lines: 120
    - path: "frontend/src/pages/HomePage.test.tsx"
      provides: "Tests for all five sections and key content"
      min_lines: 60
  key_links:
    - from: "frontend/src/pages/HomePage.tsx"
      to: "/search"
      via: "React Router Link wrapping shadcn Button in Final CTA section"
      pattern: "Link.*to=./search."
    - from: "frontend/src/pages/HomePage.tsx"
      to: "frontend/src/components/ui/card.tsx"
      via: "import { Card, CardHeader, CardTitle, CardDescription, CardContent }"
      pattern: "import.*Card.*from.*@/components/ui/card"
    - from: "frontend/src/pages/HomePage.tsx"
      to: "frontend/src/components/ui/button.tsx"
      via: "import { Button }"
      pattern: "import.*Button.*from.*@/components/ui/button"
---

<objective>
Replace the placeholder HomePage with a full tutorial landing page that teaches new students how Gaucho Score works and guides them to start searching.

Purpose: A first-time visitor who has never used the app should be able to understand the scoring system, read what each factor means, follow a clear usage guide, and confidently click through to search -- all from the home page alone.

Output: Complete HomePage.tsx with five sections (Hero, Score Breakdown, Factor Cards, Usage Guide, Final CTA) and updated test file covering all content and navigation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-tutorial-landing-page/07-CONTEXT.md
@.planning/phases/07-tutorial-landing-page/07-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From frontend/src/components/ui/card.tsx:
```typescript
export { Card, CardHeader, CardFooter, CardTitle, CardAction, CardDescription, CardContent }
// Card: accepts className, size ("default" | "sm")
// CardTitle: renders <div> with font-heading text-base leading-snug font-medium
// CardDescription: renders <div> with text-sm text-muted-foreground
// CardContent: renders <div> with px-4
// CardHeader: renders <div> with grid layout, px-4
```

From frontend/src/components/ui/button.tsx:
```typescript
export { Button, buttonVariants }
// Button: variant ("default" | "outline" | "secondary" | "ghost" | "destructive" | "link")
//         size ("default" | "xs" | "sm" | "lg" | "icon" | "icon-xs" | "icon-sm" | "icon-lg")
//         asChild?: boolean (uses Slot.Root from radix-ui)
// size="lg" gives h-9 -- for 44px+ touch target, add explicit min-h-[44px] or py padding
```

From frontend/src/pages/HomePage.tsx (current placeholder):
```typescript
import { useEffect } from 'react'
import { Link } from 'react-router-dom'
import { Button } from '@/components/ui/button'
// Pattern: useEffect sets document.title, Link wraps Button for navigation
```

From frontend/src/pages/HomePage.test.tsx (current tests):
```typescript
// Pattern: render(<MemoryRouter><HomePage /></MemoryRouter>)
// Queries: screen.getByRole('heading', {name: ...}), screen.getByRole('link', {name: ...})
// Checks: toBeInTheDocument(), toHaveAttribute('href', '/search'), document.title
```

CSS tokens from frontend/src/index.css (score bar segment colors):
```css
--primary: oklch(0.5109 0.0861 186.39);    /* GPA segment - darkest teal */
--secondary: oklch(0.6002 0.1038 184.70);  /* Quality segment */
--chart-4: oklch(0.7500 0.1000 180.00);    /* Difficulty segment */
--ring: oklch(0.8549 0.1251 181.07);       /* Sentiment segment - lightest teal */
--accent: oklch(0.6658 0.1574 58.32);      /* Amber - CTA button only */
--background: oklch(0.9836 0.0142 180.72); /* Page background */
--card: oklch(1.0000 0.0000 0);            /* Card background */
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write tests for tutorial landing page content and navigation</name>
  <files>frontend/src/pages/HomePage.test.tsx</files>
  <read_first>
    - frontend/src/pages/HomePage.test.tsx
    - .planning/phases/07-tutorial-landing-page/07-UI-SPEC.md (Copywriting Contract table -- all exact strings to assert)
    - .planning/phases/07-tutorial-landing-page/07-CONTEXT.md (locked decisions)
  </read_first>
  <behavior>
    - Test: Hero heading "Find the Best Professor for Any UCSB Course" renders as h1
    - Test: Hero subtext renders with the exact copy from UI-SPEC
    - Test: Score breakdown heading "How Gaucho Score Works" renders as h2
    - Test: Score bar has 4 segments with aria-labels "GPA: 25%", "Quality: 25%", "Difficulty: 25%", "Sentiment: 25%"
    - Test: Score bar container has role="img" with appropriate aria-label
    - Test: Factor cards heading "What Each Factor Means" renders as h2
    - Test: All 4 factor card titles render: "GPA", "Quality", "Difficulty", "Sentiment"
    - Test: Each factor card shows its example value ("3.45 / 4.00", "4.2 / 5.0", "2.8 / 5.0", "72% positive")
    - Test: Usage guide heading "How to Use Gaucho Course Optimizer" renders as h2
    - Test: All 3 step titles render: "Search a Course", "Compare Professors", "Adjust Your Priorities"
    - Test: Final CTA heading "Ready to Find Your Professor?" renders as h2
    - Test: "Start Searching" link points to /search (existing test -- keep)
    - Test: document.title is "Home | Gaucho Course Optimizer" (existing test -- keep)
  </behavior>
  <action>
Replace the entire contents of HomePage.test.tsx. Keep the existing MemoryRouter + render pattern. Structure tests in describe blocks by section: "Hero", "Score Breakdown", "Factor Cards", "Usage Guide", "Final CTA", plus a top-level "page metadata" group for document.title.

For the score bar accessibility tests, query by role="img" for the container and use getAllByLabelText with regex patterns for the segments.

For factor cards, query by heading role (h3 level) for card titles. For example values, use getByText with the exact example strings from UI-SPEC.

For the usage guide, query step titles by text content.

For the CTA, the existing link test pattern works: getByRole('link', {name: /start searching/i}) with href check.

All tests should FAIL initially since the current HomePage.tsx is a placeholder with none of this content. Run tests to confirm RED state before moving to Task 2.

IMPORTANT: Use `screen.getByRole('heading', { level: N })` or `screen.getByText()` -- do NOT use snapshot tests. Each test should assert a specific piece of content or accessibility attribute.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/HomePage.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - HomePage.test.tsx contains at least 13 test cases covering all 5 sections
    - Tests use getByRole, getByText, getAllByLabelText -- no snapshot tests
    - All new tests FAIL (RED state) because HomePage.tsx is still the placeholder
    - The 3 existing test behaviors (hero heading, start searching link, document.title) are preserved in the new test file
    - Test file imports MemoryRouter wrapper pattern consistent with existing codebase tests
  </acceptance_criteria>
  <done>Test file committed with all tests failing against the placeholder HomePage. RED state confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Implement full tutorial landing page in HomePage.tsx</name>
  <files>frontend/src/pages/HomePage.tsx</files>
  <read_first>
    - .planning/phases/07-tutorial-landing-page/07-UI-SPEC.md (FULL file -- every section spec, every Tailwind class, every copy string)
    - .planning/phases/07-tutorial-landing-page/07-CONTEXT.md (locked decisions on section order, score bar, cards, steps)
    - frontend/src/pages/HomePage.test.tsx (the tests just written -- implement to make them pass)
    - frontend/src/index.css (CSS tokens for score bar colors)
    - frontend/src/components/ui/card.tsx (Card API -- CardHeader, CardTitle, CardDescription, CardContent)
    - frontend/src/components/ui/button.tsx (Button API -- size="lg" variant, asChild pattern)
  </read_first>
  <action>
Replace the entire contents of HomePage.tsx with the full tutorial landing page. Follow the UI-SPEC section-by-section:

**Page wrapper:** `max-w-4xl mx-auto px-4` for all sections except Final CTA (which is full-bleed). Keep the existing `useEffect` for document.title.

**Section 1 -- Hero:**
- Centered text, `py-16` top/bottom spacing
- `<h1>` with `font-heading font-bold text-[28px] leading-tight` -- copy: "Find the Best Professor for Any UCSB Course"
- `<p>` with `text-base text-muted-foreground` -- copy: exact subtext from UI-SPEC Copywriting Contract
- No CTA button in hero (CTA is in Final CTA section only)

**Section 2 -- Score Breakdown:**
- `py-16` spacing, centered heading
- `<h2>` "How Gaucho Score Works" with `font-heading font-bold text-[20px]`
- Subtext: "Each score is a 0-100 composite..." (exact UI-SPEC copy)
- Stacked bar: `flex w-full h-10 rounded-lg overflow-hidden ring-1 ring-foreground/10`
- Container div: `role="img" aria-label="Gaucho Score breakdown: four equally weighted factors"`
- 4 segments, each `flex-1`:
  - GPA: `bg-primary` with `aria-label="GPA: 25%"`
  - Quality: `bg-secondary` with `aria-label="Quality: 25%"`
  - Difficulty: `bg-[oklch(0.75_0.10_180)]` with `aria-label="Difficulty: 25%"`
  - Sentiment: `bg-[oklch(0.8549_0.1251_181.07)]` with `aria-label="Sentiment: 25%"`
- Labels row below: `flex justify-between mt-2`, each label `text-[14px] text-muted-foreground text-center flex-1`
  - "GPA . 25%", "Quality . 25%", "Difficulty . 25%", "Sentiment . 25%" (use middle dot character)
- Score badge example: "Example: a score of 78/100" with `text-sm font-medium text-foreground`

**Section 3 -- Factor Cards:**
- `py-16` spacing, centered heading
- `<h2>` "What Each Factor Means" with `font-heading font-bold text-[20px]`
- Grid: `grid grid-cols-1 sm:grid-cols-2 gap-6`
- Each card uses shadcn `Card` with `CardHeader` + `CardContent`:
  - CardHeader contains the lucide icon (24px, `text-primary`) and CardTitle (factor name as `<h3>`)
  - CardDescription in CardHeader: jargon-free definition from UI-SPEC
  - CardContent: example value prefixed "Example:" with `text-sm font-medium text-foreground`
- Factor cards in order (per UI-SPEC table):
  1. GPA -- `GraduationCap` icon -- definition and "Example: 3.45 / 4.00"
  2. Quality -- `Star` icon -- definition and "Example: 4.2 / 5.0"
  3. Difficulty -- `TrendingDown` icon -- definition and "Example: 2.8 / 5.0"
  4. Sentiment -- `MessageCircle` icon -- definition and "Example: 72% positive"

**Section 4 -- Usage Guide:**
- `py-16` spacing, centered heading
- `<h2>` "How to Use Gaucho Course Optimizer" with `font-heading font-bold text-[20px]`
- Semantic `<ol>` wrapping step containers (per accessibility spec)
- Layout: `flex flex-col md:flex-row items-start justify-between gap-8` on the ol
- Each step is an `<li>` with `flex flex-col items-center text-center flex-1`
- Step circle: `w-10 h-10 rounded-full bg-primary text-primary-foreground flex items-center justify-center font-heading font-bold text-base` with `aria-hidden="true"`
- Step icon below circle: lucide icon 20px `text-primary`
- Step title: `font-heading font-medium text-base mt-3`
- Step body: `text-sm text-muted-foreground mt-1`
- Connector lines between steps (desktop only): `hidden md:block` divs with `flex-1 h-px bg-border` placed between li elements in the flex row, with `mt-5` to align with circle centers
- Steps:
  1. `Search` icon -- "Search a Course" -- body from UI-SPEC
  2. `BarChart2` icon -- "Compare Professors" -- body from UI-SPEC
  3. `SlidersHorizontal` icon -- "Adjust Your Priorities" -- body from UI-SPEC

**Section 5 -- Final CTA Banner:**
- FULL-BLEED: This section breaks out of the max-w-4xl container. Wrap it outside the container div, OR close and reopen the container. Use `bg-primary` on a full-width wrapper, then inner `max-w-4xl mx-auto px-4 py-16 text-center`.
- `<h2>` "Ready to Find Your Professor?" with `font-heading font-bold text-[28px] text-primary-foreground`
- Subtext: "Search any UCSB course and see ranked results in seconds." with `text-base text-primary-foreground/80`
- CTA: `<Link to="/search">` wrapping `<Button>` with `className="bg-accent hover:bg-accent/90 text-accent-foreground min-h-[44px] px-6"` and `size="lg"` -- label "Start Searching"
- The min-h-[44px] and px-6 ensure WCAG 2.5.5 touch target compliance (shadcn lg is only h-9 = 36px)

**Imports needed:**
- `useEffect` from react
- `Link` from react-router-dom
- `Button` from @/components/ui/button
- `Card, CardHeader, CardTitle, CardDescription, CardContent` from @/components/ui/card
- `GraduationCap, Star, TrendingDown, MessageCircle, Search, BarChart2, SlidersHorizontal` from lucide-react

**Page structure pattern:**
```
<div> (outer wrapper for full-bleed CTA)
  <div className="max-w-4xl mx-auto px-4"> (content container)
    {Hero section}
    {Score Breakdown section}
    {Factor Cards section}
    {Usage Guide section}
  </div>
  <div className="bg-primary"> (full-bleed CTA)
    <div className="max-w-4xl mx-auto px-4 py-16 text-center">
      ...CTA content...
    </div>
  </div>
</div>
```

After implementation, run the full test suite to confirm GREEN state.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/HomePage.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - ALL tests from Task 1 pass (GREEN state)
    - HomePage.tsx contains exactly 5 sections in the locked order: Hero, Score Breakdown, Factor Cards, Usage Guide, Final CTA
    - All copy matches the UI-SPEC Copywriting Contract table exactly (no paraphrasing)
    - Score bar uses the 4 correct CSS token colors: bg-primary, bg-secondary, bg-[oklch(0.75_0.10_180)], bg-[oklch(0.8549_0.1251_181.07)]
    - Factor cards use shadcn Card component (not custom divs)
    - All aria-labels and role attributes match UI-SPEC Accessibility table
    - Heading hierarchy is correct: h1 (hero) then h2 (section headings) then h3 (card titles)
    - CTA button links to /search
    - CTA button meets 44px minimum touch target
    - Factor grid is responsive: 1-col below 640px, 2x2 on sm+
    - Usage guide steps are horizontal on md+, vertical on mobile
    - Connector lines between steps are hidden on mobile, visible on md+
    - Full test suite passes (all 64+ existing tests + new HomePage tests)
    - TypeScript compiles cleanly: npx tsc --noEmit
    - Production build succeeds: npm run build
  </acceptance_criteria>
  <done>All HomePage tests pass. Full test suite green. TypeScript clean. Build succeeds. Tutorial landing page renders all 5 sections with correct content, accessibility, and responsive behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This phase is purely static content -- no user input, no data fetching, no API calls, no form submissions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Link href | accept | Link `to="/search"` is a static internal route -- no user-controlled input, no injection vector |
| T-07-02 | Information Disclosure | Page content | accept | All content is intentionally public tutorial material -- no sensitive data |

No security mitigations required. This phase adds only static, read-only content with no trust boundaries crossed.
</threat_model>

<verification>
1. `cd frontend && npx vitest run --reporter=verbose` -- all tests pass (64 existing + new HomePage tests)
2. `cd frontend && npx tsc --noEmit` -- zero TypeScript errors
3. `cd frontend && npm run build` -- production build succeeds
4. HomePage.tsx contains all 5 sections in locked order with exact UI-SPEC copy
5. Score bar renders 4 teal-shade segments with correct aria-labels
6. Factor cards render 4 definitions with jargon-free text and example values
7. Usage guide renders 3 steps with icons and connector lines
8. CTA button navigates to /search
</verification>

<success_criteria>
- Student landing on the home page can see a visual 4-segment score breakdown bar
- Student can read all 4 factor definitions (GPA, Quality, Difficulty, Sentiment) with real example values
- Student can follow a 3-step usage guide (Search, Compare, Adjust)
- Student can click "Start Searching" to navigate to /search
- All text matches the locked UI-SPEC copywriting contract exactly
- Heading hierarchy, ARIA attributes, and touch targets meet accessibility requirements
- Page is responsive: cards stack on mobile, steps go vertical on mobile, connector lines hide on mobile
- Full test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/07-tutorial-landing-page/07-01-SUMMARY.md`
</output>
