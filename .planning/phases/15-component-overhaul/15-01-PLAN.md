---
phase: 15-component-overhaul
plan: 15-01
title: Component Visual Overhaul
type: implementation
wave: 1
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04, COMP-05, COMP-06, COMP-07, COMP-08]
depends_on: [14-01]
---

# Phase 15 Plan 01: Component Visual Overhaul

## Objective

Redesign every user-facing component to use the Royal Blue + Snow White design tokens (Phase 12), accessibility patterns (Phase 13), and animation utilities (Phase 14). Replace any remaining legacy teal/amber colors, ensure consistent Inter typography, spacing, and border-radius across all components.

## Context

- @frontend/src/index.css -- Design tokens, animation utilities, focus/a11y styles
- @frontend/src/components/ProfessorCard.tsx -- COMP-01
- @frontend/src/components/Navbar.tsx -- COMP-02
- @frontend/src/components/Breadcrumbs.tsx -- COMP-03
- @frontend/src/components/CourseSearch.tsx -- COMP-04
- @frontend/src/components/GradeChart.tsx -- COMP-05
- @frontend/src/components/GpaTrendChart.tsx -- COMP-05
- @frontend/src/components/MobileMenu.tsx -- COMP-06
- @frontend/src/pages/HomePage.tsx -- COMP-07
- @frontend/src/components/SentimentBadge.tsx -- COMP-08
- @frontend/src/components/WeightToggles.tsx -- styling
- @frontend/src/pages/CoursePage.tsx -- layout
- @frontend/src/pages/SearchPage.tsx -- styling

## Tasks

### Task 1: Navigation components (Navbar, Breadcrumbs, MobileMenu, SearchPage)
type: auto

Update navigation-layer components to use Royal Blue tokens and Inter typography consistently:

**Navbar.tsx (COMP-02):**
- Add Inter font-sans class to nav links for explicit Inter usage
- Add btn-press class to CoursePick brand link for tactile feedback
- Ensure Royal Blue bg-primary is in place (verify, already set via tokens)

**Breadcrumbs.tsx (COMP-03):**
- Update border to use `border-primary/20` (blue-tinted #DBEAFE-like border)
- Active breadcrumb item: change from `text-accent` to `text-primary font-semibold` for Royal Blue active state
- Add subtle bg tint using `bg-primary/5` instead of `bg-muted/50`

**MobileMenu.tsx (COMP-06):**
- Add `border-l-4 border-primary` accent on active nav items
- Add `rounded-md` on nav links for consistent border-radius
- Ensure SheetTitle uses font-heading (already set, verify)

**CourseSearch.tsx (COMP-04):**
- Add `ring-primary` focus styling class and border-primary/20 border
- Style CommandItem with hover:bg-primary/10 for Royal Blue hover tint

**SearchPage.tsx:**
- Update heading to use `text-primary` for Royal Blue heading color

**Done criteria:** Navbar renders with Royal Blue background, breadcrumbs have blue-tinted border and primary-colored active item, mobile Sheet has accent borders, search input/dropdown use Royal Blue focus and hover. All 100 tests pass.

### Task 2: Professor cards, charts, and badges
type: auto

Update data display components with new color system:

**ProfessorCard.tsx (COMP-01):**
- Add `border-l-4 border-primary/30` left accent stripe on card
- Update CardContent padding from p-4 to p-5 for increased spacing
- Score badge: ensure green-600/yellow-600/red-600 colors are correct (already set, verify)
- "Actively Teaching" badge: already uses bg-primary, verify
- Tag badges: add `bg-primary/10 text-primary` styling for blue-tinted tags
- "Show details" / "Hide details" links: add `text-primary` color
- Quarter badges: add `border-primary/30` outline tint

**GradeChart.tsx (COMP-05):**
- Use CSS variable for bar fill: `var(--chart-1)` token via hsl, or keep #2563EB (already Royal Blue)
- Add explicit fontFamily: 'Inter Variable' to XAxis/YAxis tick props
- Style title heading with text-primary color

**GpaTrendChart.tsx (COMP-05):**
- Add explicit fontFamily: 'Inter Variable' to XAxis/YAxis tick props
- Keep stroke #2563EB (already Royal Blue)

**SentimentBadge.tsx (COMP-08):**
- Update colors to use blue-family neutral instead of orange: Neutral -> `bg-blue-100 text-blue-800`
- Positive stays green, Negative stays red (semantic colors preserved)
- Add `rounded-full` for pill shape consistency

**WeightToggles.tsx:**
- Add `text-primary` to section heading for consistency

**CoursePage.tsx:**
- Add `border-primary/20` to sidebar sticky panel for subtle delineation
- Update cold-start warning banner to use `border-primary/30 bg-primary/5 text-primary`

**Done criteria:** Professor cards have blue accent stripe, increased spacing, blue-tinted tags. Charts use Inter font in axis labels. Sentiment badges use blue-family neutral. All 100 tests pass.

### Task 3: Tutorial landing page redesign
type: auto

Full visual overhaul of HomePage.tsx (COMP-07):

**Score breakdown section:**
- Replace legacy teal oklch colors (180/181 hue) with blue-family oklch colors
  - Third bar: change `oklch(0.75_0.10_180)` to `oklch(0.707_0.165_254.624)` (chart-3 token)
  - Fourth bar: change `oklch(0.8549_0.1251_181.07)` to `oklch(0.809_0.105_251.813)` (chart-4 token)
- Add `rounded-full` to individual score bar segments for softer look
- Score bar labels: add `font-medium` for improved readability

**Factor cards:**
- Add `card-hover` class to factor cards for hover elevation
- Add `border-l-4 border-primary/30` left accent stripe matching professor cards
- Icon color: already text-primary, verify

**Usage guide steps:**
- Step number circles: already bg-primary, verify
- Step icons: already text-primary, verify
- Add subtle `bg-card rounded-xl p-6 shadow-sm` wrapper around each step for card feel
- Connector lines: update from `bg-border` to `bg-primary/20` for blue tint

**CTA banner:**
- Already uses bg-primary, verify
- Button: change from `bg-accent` to `bg-white text-primary hover:bg-white/90` for better contrast on Royal Blue background
- Add `btn-press` class for button press feedback

**Hero section:**
- Add `text-primary` to h1 for Royal Blue heading
- Add subtle visual separator below hero (e.g., `border-b border-primary/10`)

**Done criteria:** No legacy teal oklch colors remain. Score breakdown uses blue-family palette. Factor cards have hover elevation and accent stripe. CTA button has proper contrast. All 100 tests pass.

## Verification

```bash
cd frontend && npx vitest run
```

All 100 tests must pass after all three tasks.

## Success Criteria

1. No legacy teal/amber hex values or teal-hue oklch colors remain in any component
2. All navigation components use Royal Blue tokens consistently
3. Professor cards have updated spacing, accent stripe, and blue-tinted badges
4. Charts use Inter font in axis labels
5. Tutorial page uses blue-family palette throughout with no teal remnants
6. All 100+ existing tests pass
