---
phase: 06-navigation-routing
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - frontend/src/components/Breadcrumbs.tsx
  - frontend/src/components/Breadcrumbs.test.tsx
  - frontend/src/components/MobileMenu.tsx
  - frontend/src/components/MobileMenu.test.tsx
  - frontend/src/layouts/Layout.tsx
  - frontend/src/components/Navbar.tsx
autonomous: false
requirements: [NAV-02, NAV-03]
must_haves:
  truths:
    - "Student on /search sees breadcrumbs: Home > Search"
    - "Student on /courses/:id sees breadcrumbs: Home > Search > CMPSC 130A (actual course code)"
    - "Student on / (Home) sees no breadcrumbs"
    - "Student on mobile sees a hamburger icon in the navbar"
    - "Student can tap hamburger to open a Sheet drawer sliding from the right"
    - "Student can tap a link in the Sheet and it auto-closes after navigation"
    - "Sheet closes on Escape key press and outside tap"
  artifacts:
    - path: "frontend/src/components/Breadcrumbs.tsx"
      provides: "Route-conditional breadcrumb strip"
      exports: ["default (Breadcrumbs)"]
    - path: "frontend/src/components/MobileMenu.tsx"
      provides: "Sheet-based mobile navigation drawer"
      exports: ["default (MobileMenu)"]
  key_links:
    - from: "frontend/src/layouts/Layout.tsx"
      to: "frontend/src/components/Breadcrumbs.tsx"
      via: "Breadcrumbs rendered between Navbar and Outlet"
      pattern: "<Breadcrumbs"
    - from: "frontend/src/components/Navbar.tsx"
      to: "frontend/src/components/MobileMenu.tsx"
      via: "MobileMenu rendered inside Navbar's mobile slot"
      pattern: "<MobileMenu"
    - from: "frontend/src/components/MobileMenu.tsx"
      to: "@/components/ui/sheet"
      via: "Sheet + SheetClose asChild wrapping Links"
      pattern: "SheetClose asChild"
    - from: "frontend/src/components/Breadcrumbs.tsx"
      to: "useLocation().pathname"
      via: "Route detection via pathname matching"
      pattern: "useLocation"
---

<objective>
Add breadcrumb navigation and mobile hamburger menu to complete the navigation system. Breadcrumbs render conditionally below the navbar on interior pages. The mobile menu uses the existing shadcn Sheet component sliding from the right with auto-close on link selection.

Purpose: Delivers NAV-02 (breadcrumb location awareness) and NAV-03 (mobile hamburger navigation). Completes the full navigation system started in Plan 01.

Output: Breadcrumbs.tsx and MobileMenu.tsx created, Layout.tsx updated to include Breadcrumbs, Navbar.tsx updated to include MobileMenu.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-navigation-routing/06-CONTEXT.md
@.planning/phases/06-navigation-routing/06-RESEARCH.md
@.planning/phases/06-navigation-routing/06-UI-SPEC.md
@.planning/phases/06-navigation-routing/06-01-SUMMARY.md

@frontend/src/layouts/Layout.tsx
@frontend/src/components/Navbar.tsx
@frontend/src/components/ui/sheet.tsx
@frontend/src/index.css

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan builds on. -->

From frontend/src/layouts/Layout.tsx (created in Plan 01):
```tsx
// Layout renders Navbar + Outlet. This plan adds Breadcrumbs between them.
import { Outlet } from 'react-router-dom'
import Navbar from '@/components/Navbar'

export default function Layout() {
  return (
    <div className="min-h-screen flex flex-col">
      <Navbar />
      {/* Breadcrumbs component will be added here in Plan 02 */}
      <main className="flex-1">
        <Outlet />
      </main>
    </div>
  )
}
```

From frontend/src/components/Navbar.tsx (created in Plan 01):
```tsx
// Navbar has a mobile slot div: <div className="flex md:hidden">{children}</div>
// OR: Navbar exposes a spot where MobileMenu can be composed
// This plan adds MobileMenu either as children or by importing directly in Navbar
```

From frontend/src/components/ui/sheet.tsx:
```tsx
export {
  Sheet,
  SheetTrigger,
  SheetClose,      // Wraps children with asChild to close on interaction
  SheetContent,    // side="right" default; Radix Dialog handles Escape + outside click
  SheetHeader,
  SheetFooter,
  SheetTitle,
  SheetDescription,
}
```

From frontend/src/components/CourseSearch.tsx (updated in Plan 01):
```tsx
// navigate(`/courses/${course.id}`, { state: { courseCode: course.code } })
// Breadcrumbs reads location.state?.courseCode for the course breadcrumb label
```

From react-router-dom v7.13.2:
```tsx
import { useLocation, Link } from 'react-router-dom'
// useLocation() returns { pathname, state, search, hash, key }
// state?.courseCode contains the course code passed from CourseSearch
// IMPORTANT: useParams() returns {} in Layout-level components -- parse pathname instead
```

From lucide-react (installed):
```tsx
import { Menu, ChevronRight } from 'lucide-react'
// Menu: hamburger icon for mobile trigger
// ChevronRight: breadcrumb separator
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Breadcrumbs component and wire into Layout</name>
  <files>
    frontend/src/components/Breadcrumbs.tsx
    frontend/src/components/Breadcrumbs.test.tsx
    frontend/src/layouts/Layout.tsx
  </files>
  <read_first>
    frontend/src/layouts/Layout.tsx
    frontend/src/components/ui/sheet.tsx
    frontend/src/index.css
    frontend/src/components/CourseSearch.tsx
    .planning/phases/06-navigation-routing/06-UI-SPEC.md
    .planning/phases/06-navigation-routing/06-RESEARCH.md
  </read_first>
  <behavior>
    - At pathname "/": Breadcrumbs returns null (no breadcrumbs on Home)
    - At pathname "/search": renders "Home" link + ChevronRight + "Search" (active, not a link)
    - At pathname "/courses/42" with location.state.courseCode = "CMPSC 130A": renders "Home" link + chevron + "Search" link + chevron + "CMPSC 130A" (active)
    - At pathname "/courses/42" with NO location.state: renders "Home" link + chevron + "Search" link + chevron + "Course Results" (fallback label)
    - Breadcrumb container has aria-label="Breadcrumb" on nav element
    - Breadcrumb list uses ol element
    - Chevron separators have aria-hidden="true"
    - Active (last) breadcrumb item has text-accent font-medium classes and is NOT a link
    - Inactive breadcrumb items are Link elements with text-muted-foreground hover:text-foreground
  </behavior>
  <action>
    Create `frontend/src/components/Breadcrumbs.tsx`:
    - Import `useLocation`, `Link` from `react-router-dom`
    - Import `ChevronRight` from `lucide-react`
    - Call `useLocation()` to get `pathname` and `state`
    - If `pathname === '/'`, return `null`
    - If `pathname === '/search'`, return breadcrumb: Home (link to /) > Search (active)
    - If `pathname` matches `/^\/courses\/(.+)$/`:
      - Read `(state as { courseCode?: string })?.courseCode` for the label
      - Fallback to "Course Results" if courseCode is not in state (direct URL access)
      - Return breadcrumb: Home (link to /) > Search (link to /search) > {courseLabel} (active)
    - Otherwise return `null`
    - Container: `<nav aria-label="Breadcrumb" className="bg-muted/50 border-b border-border px-4 md:px-6 py-2">`
    - List: `<ol className="flex items-center gap-1 text-sm">`
    - Separator: `<li aria-hidden="true"><ChevronRight className="h-3.5 w-3.5 text-muted-foreground" /></li>`
    - Inactive item: `<li><Link to="..." className="text-muted-foreground hover:text-foreground transition-colors">...</Link></li>`
    - Active item: `<li aria-current="page" className="text-accent font-medium">...</li>`

    Create `frontend/src/components/Breadcrumbs.test.tsx`:
    - Use MemoryRouter with initialEntries for each route scenario
    - Test: at "/", renders nothing (queryByRole('navigation') returns null)
    - Test: at "/search", renders "Home" and "Search" text, "Home" is a link, "Search" is not a link
    - Test: at "/courses/42" with state { courseCode: "CMPSC 130A" }, renders "Home" > "Search" > "CMPSC 130A"
    - Test: at "/courses/42" with no state, renders fallback "Course Results" as last breadcrumb
    - Test: breadcrumb nav has aria-label="Breadcrumb"

    Update `frontend/src/layouts/Layout.tsx`:
    - Import Breadcrumbs from `@/components/Breadcrumbs`
    - Add `<Breadcrumbs />` between `<Navbar />` and `<main>`
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/Breadcrumbs.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - Breadcrumbs.tsx uses useLocation() (not useParams()) for route detection
    - At "/" pathname, component returns null
    - At "/search", renders nav with aria-label="Breadcrumb" containing "Home" link and "Search" active text
    - At "/courses/42" with state.courseCode="CMPSC 130A", last breadcrumb shows "CMPSC 130A"
    - At "/courses/42" without state, last breadcrumb shows "Course Results" as fallback
    - ChevronRight separators have aria-hidden="true"
    - Active breadcrumb item has classes "text-accent font-medium"
    - Layout.tsx imports and renders Breadcrumbs between Navbar and main/Outlet
    - All Breadcrumbs tests pass
  </acceptance_criteria>
  <done>Breadcrumbs component renders route-conditional breadcrumb trail with correct formatting, accessibility attributes, and courseCode from router state. Layout updated to include Breadcrumbs. All tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create MobileMenu component and wire into Navbar</name>
  <files>
    frontend/src/components/MobileMenu.tsx
    frontend/src/components/MobileMenu.test.tsx
    frontend/src/components/Navbar.tsx
  </files>
  <read_first>
    frontend/src/components/Navbar.tsx
    frontend/src/components/ui/sheet.tsx
    frontend/src/index.css
    .planning/phases/06-navigation-routing/06-UI-SPEC.md
    .planning/phases/06-navigation-routing/06-RESEARCH.md
  </read_first>
  <behavior>
    - MobileMenu renders a Sheet component with SheetTrigger containing a hamburger Menu icon button
    - Hamburger button has aria-label="Open navigation menu" and min-h-[44px] min-w-[44px] touch target
    - SheetContent opens from side="right"
    - SheetHeader contains SheetTitle "Gaucho Course Optimizer" in font-heading text-base font-bold
    - Sheet contains two nav links: "Home" (to /) and "Search" (to /search)
    - Each link is wrapped in SheetClose asChild so the Sheet closes on link click
    - Links have min-h-[44px] for touch targets with py-3 px-4 text-base
    - Escape key and outside tap close the Sheet (handled by Radix automatically)
    - Active link in Sheet has font-bold text-primary
  </behavior>
  <action>
    Create `frontend/src/components/MobileMenu.tsx`:
    - Import Sheet, SheetTrigger, SheetContent, SheetClose, SheetHeader, SheetTitle from `@/components/ui/sheet`
    - Import NavLink from `react-router-dom` (for active state detection in the Sheet)
    - Import Menu from `lucide-react`
    - Render `<Sheet>` (uncontrolled mode -- state managed internally)
    - `<SheetTrigger asChild>`:
      - `<button aria-label="Open navigation menu" className="p-2 min-h-[44px] min-w-[44px] flex items-center justify-center text-primary-foreground">`
      - `<Menu className="h-5 w-5" />`
    - `<SheetContent side="right">`:
      - `<SheetHeader><SheetTitle className="font-heading text-base font-bold">Gaucho Course Optimizer</SheetTitle></SheetHeader>`
      - `<nav className="flex flex-col mt-4">`
      - Two links, each wrapped: `<SheetClose asChild><NavLink to="/" end className={({ isActive }) => \`py-3 px-4 text-base font-sans min-h-[44px] flex items-center \${isActive ? 'font-bold text-primary' : 'text-foreground hover:text-primary'}\`}>Home</NavLink></SheetClose>`
      - Same pattern for "/search" link (no `end` prop needed)

    Create `frontend/src/components/MobileMenu.test.tsx`:
    - Use MemoryRouter with initialEntries
    - Test: renders hamburger button with aria-label "Open navigation menu"
    - Test: clicking hamburger opens Sheet (SheetTitle "Gaucho Course Optimizer" becomes visible)
    - Test: Sheet contains "Home" and "Search" links
    - Test: clicking a link in the Sheet triggers SheetClose (after click, Sheet content is no longer in DOM -- use waitForElementToBeRemoved or check visibility)

    Update `frontend/src/components/Navbar.tsx`:
    - Import MobileMenu from `@/components/MobileMenu`
    - Add `<div className="flex md:hidden"><MobileMenu /></div>` on the right side of navbar, adjacent to (but separate from) the desktop nav links div
    - Ensure the mobile slot is only visible below md breakpoint
    - Desktop links remain in `<div className="hidden md:flex items-center gap-4">`
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/MobileMenu.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - MobileMenu.tsx uses Sheet from @/components/ui/sheet
    - SheetContent has side="right"
    - Hamburger button has aria-label="Open navigation menu"
    - Hamburger button has min-h-[44px] and min-w-[44px] classes
    - Each link inside Sheet is wrapped with SheetClose asChild
    - SheetTitle text is "Gaucho Course Optimizer"
    - NavLink in Sheet uses className callback with isActive for font-bold text-primary
    - Navbar.tsx imports and renders MobileMenu inside a div with "flex md:hidden"
    - All MobileMenu tests pass
  </acceptance_criteria>
  <done>MobileMenu renders a Sheet-based drawer with two nav links that auto-close on selection. Hamburger button meets 44px touch target. Navbar integrates MobileMenu in mobile-only slot. All tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete navigation system</name>
  <files>None -- verification only</files>
  <read_first>
    .planning/phases/06-navigation-routing/06-UI-SPEC.md
  </read_first>
  <action>
    Start the dev server with `cd frontend && npm run dev` and perform manual visual verification of the complete navigation system against the UI-SPEC contract. All automated tests should already be passing from Tasks 1 and 2. This task is a human checkpoint to confirm visual appearance, interaction feel, and end-to-end navigation flow.
  </action>
  <what-built>
    Complete navigation system with:
    1. Sticky top navbar (Deep Teal background, Amber brand text, Home/Search links)
    2. Breadcrumbs on interior pages (Home > Search, Home > Search > Course Code)
    3. Mobile hamburger menu with Sheet drawer sliding from right
    4. Placeholder Home page with hero heading and "Start Searching" CTA
    5. Route restructure: / = Home, /search = Search, /courses/:id = Course Results
  </what-built>
  <how-to-verify>
    1. Run `cd frontend && npm run dev` to start the dev server
    2. Visit http://localhost:5173/ -- should see:
       - Deep Teal navbar at top with "Gaucho Course Optimizer" in amber
       - "Home" and "Search" nav links on desktop (Home should be bold/active)
       - No breadcrumbs on this page
       - Hero heading: "Find the Best Professor for Any UCSB Course"
       - Amber "Start Searching" button
    3. Click "Start Searching" -- should navigate to /search:
       - Navbar still visible, "Search" link now bold/active
       - Breadcrumbs: "Home > Search" visible below navbar
       - Search input visible
    4. Type "physics" in search, select a course -- should navigate to /courses/:id:
       - Breadcrumbs: "Home > Search > PHYSICS 1" (or actual course code)
       - Navbar still visible
       - Professor cards visible, sidebar not hidden under navbar
    5. Click browser Back button -- should return to /search
    6. Resize browser to mobile (< 768px):
       - Desktop nav links hidden, hamburger icon visible
       - Tap hamburger -- Sheet slides from right with "Home" and "Search" links
       - Tap a link -- Sheet closes and navigates
    7. Paste http://localhost:5173/search directly -- should load search page correctly (no 404)
  </how-to-verify>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - Navbar is visually sticky at top with Deep Teal background on all pages
    - Brand text "Gaucho Course Optimizer" displays in amber color
    - Active NavLink is bold white, inactive is semi-transparent
    - Breadcrumbs appear on /search and /courses/:id but NOT on /
    - Mobile hamburger icon visible below 768px, desktop links above
    - Sheet drawer slides from right and closes on link click
    - CoursePage sidebar does not overlap navbar when scrolling
    - All automated tests pass
  </acceptance_criteria>
  <done>User has visually confirmed the complete navigation system matches the UI-SPEC contract across desktop and mobile viewports.</done>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL pathname to breadcrumb display | Pathname segments rendered as text in breadcrumbs -- React JSX escapes automatically |
| Router state to breadcrumb display | courseCode from navigate state rendered in JSX -- React JSX escapes automatically |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Information Disclosure | Breadcrumbs courseCode | accept | Course codes are public university data, no PII; React JSX escaping prevents XSS |
| T-06-05 | Tampering | Router state manipulation | accept | courseCode is display-only; tampering only affects breadcrumb label text, not functionality; no security impact |
| T-06-06 | Denial of Service | Sheet focus trap | accept | Radix Dialog primitive handles focus trap correctly; no custom code needed |
</threat_model>

<verification>
1. `cd frontend && npx vitest run --reporter=verbose` -- all tests pass (Breadcrumbs + MobileMenu + all existing)
2. `cd frontend && npx tsc --noEmit` -- no TypeScript errors
3. `cd frontend && npm run build` -- production build succeeds
4. Manual: Breadcrumbs show correct path on /search and /courses/:id
5. Manual: No breadcrumbs on /
6. Manual: Mobile hamburger opens Sheet, links navigate and close Sheet
7. Manual: Escape key closes Sheet
8. Manual: Outside tap closes Sheet
</verification>

<success_criteria>
- Breadcrumbs show "Home > Search" on /search page
- Breadcrumbs show "Home > Search > {courseCode}" on /courses/:id page (with fallback to "Course Results" for direct URL access)
- No breadcrumbs render on / (Home page)
- Mobile hamburger icon visible below 768px, desktop links visible above
- Sheet drawer slides from right on hamburger tap
- All links in Sheet wrapped with SheetClose for auto-close behavior
- All existing + new tests pass with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/06-navigation-routing/06-02-SUMMARY.md`
</output>
