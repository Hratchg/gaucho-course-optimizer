---
phase: 03-react-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/tsconfig.node.json
  - frontend/index.html
  - frontend/src/index.css
  - frontend/src/lib/utils.ts
  - frontend/components.json
autonomous: true
requirements:
  - UI-01
  - UI-10

must_haves:
  truths:
    - "Running `npm run build` in frontend/ produces a production build with no errors"
    - "frontend/src/components/ui/ contains all 7 required shadcn components"
    - "frontend/package.json contains all required runtime and dev dependencies"
  artifacts:
    - path: "frontend/package.json"
      provides: "All project dependencies installed"
      contains: "@tanstack/react-query"
    - path: "frontend/vite.config.ts"
      provides: "Vite config with Tailwind v4 plugin and @ alias"
      contains: "tailwindcss()"
    - path: "frontend/src/lib/utils.ts"
      provides: "shadcn cn() utility function"
      contains: "cn"
  key_links:
    - from: "frontend/vite.config.ts"
      to: "@tailwindcss/vite"
      via: "Tailwind v4 plugin"
      pattern: "tailwindcss"
---

<objective>
Scaffold the frontend/ project from scratch -- Vite + React + TypeScript, Tailwind v4, shadcn/ui init with all 7 required components, and install all runtime and test dependencies. This plan runs CLI commands only -- no hand-written application code.

Purpose: Creates the project shell and installs all dependencies that every subsequent plan builds on. Only auto-generated files from scaffolding tools.
Output: A `frontend/` directory with all dependencies installed, shadcn/ui components available, and `npm run build` passing.
</objective>

<execution_context>
@C:/Users/hratc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hratc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-react-frontend/03-CONTEXT.md
@.planning/phases/03-react-frontend/03-RESEARCH.md
@.planning/phases/03-react-frontend/03-UI-SPEC.md

<interfaces>
<!-- No interfaces needed -- this is a pure scaffold plan with no imports from existing code -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite project, install all dependencies, initialize shadcn/ui</name>
  <files>
    frontend/package.json, frontend/vite.config.ts, frontend/tsconfig.json,
    frontend/tsconfig.app.json, frontend/tsconfig.node.json, frontend/index.html,
    frontend/src/index.css, frontend/src/lib/utils.ts, frontend/components.json
  </files>
  <read_first>
    .planning/phases/03-react-frontend/03-RESEARCH.md (Standard Stack section -- installation commands, version info)
    .planning/phases/03-react-frontend/03-UI-SPEC.md (Design System section -- required shadcn components list)
  </read_first>
  <action>
    Run these commands sequentially in the project root:

    1. Scaffold Vite + React + TypeScript:
       ```bash
       npm create vite@latest frontend -- --template react-ts
       cd frontend && npm install
       ```

    2. Install Tailwind CSS v4 (NOT v3 -- no postcss.config.js, no tailwind.config.js):
       ```bash
       npm install -D tailwindcss @tailwindcss/vite
       ```

    3. Configure vite.config.ts -- add tailwindcss() plugin AND path alias:
       ```typescript
       import { defineConfig } from 'vite'
       import react from '@vitejs/plugin-react'
       import tailwindcss from '@tailwindcss/vite'
       import path from 'path'

       export default defineConfig({
         plugins: [react(), tailwindcss()],
         resolve: {
           alias: {
             '@': path.resolve(__dirname, './src'),
           },
         },
       })
       ```

    4. Update `src/index.css` -- replace all Vite default content with:
       ```css
       @import "tailwindcss";
       ```
       (shadcn init may add more lines -- that is fine)

    5. Install @types/node (required before shadcn init):
       ```bash
       npm install -D @types/node
       ```

    6. Initialize shadcn/ui:
       ```bash
       npx shadcn@latest init
       ```
       Accept defaults. This creates `components.json`, adds CSS variables to index.css, creates `src/lib/utils.ts` with `cn()`.

    7. Add all 7 required shadcn components:
       ```bash
       npx shadcn@latest add command skeleton sheet collapsible card badge slider
       ```

    8. Install runtime dependencies:
       ```bash
       npm install @tanstack/react-query react-router-dom recharts
       ```

    9. Install test dependencies:
       ```bash
       npm install -D vitest jsdom @testing-library/react @testing-library/user-event @testing-library/jest-dom msw
       ```

    10. Update `frontend/index.html` `<title>` to "Gaucho Course Optimizer" (per UI-SPEC Copywriting Contract).

    11. Delete any Vite default files not needed: `src/App.css`, `src/assets/react.svg`, `public/vite.svg`.

    12. Do NOT modify src/main.tsx or src/App.tsx beyond cleanup of Vite boilerplate imports -- Plan 02 handles hand-written application code.

    CRITICAL: Do NOT create postcss.config.js or tailwind.config.js. Tailwind v4 uses @tailwindcss/vite plugin ONLY.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json contains "@tanstack/react-query", "react-router-dom", "recharts" in dependencies
    - frontend/package.json contains "vitest", "msw", "@testing-library/react", "tailwindcss", "@tailwindcss/vite" in devDependencies
    - frontend/vite.config.ts contains "tailwindcss()" in plugins array
    - frontend/vite.config.ts contains "'@': path.resolve(__dirname, './src')"
    - frontend/src/index.css contains '@import "tailwindcss"'
    - frontend/index.html contains "Gaucho Course Optimizer" in title tag
    - frontend/src/components/ui/ directory contains command.tsx, skeleton.tsx, sheet.tsx, collapsible.tsx, card.tsx, badge.tsx, slider.tsx
    - frontend/src/lib/utils.ts exists with cn() function (created by shadcn init)
    - frontend/components.json exists (created by shadcn init)
    - No tailwind.config.js or postcss.config.js files exist (Tailwind v4 uses @tailwindcss/vite)
    - No src/App.css or src/assets/react.svg exist (Vite boilerplate cleaned)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    Vite project scaffolded with React + TypeScript + Tailwind v4 + shadcn/ui.
    All 7 shadcn components installed. All runtime and test dependencies available.
    `npm run build` succeeds with no errors.
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npm run build` completes with no errors
2. `cd frontend && npx tsc --noEmit` has no TypeScript errors
3. `ls frontend/src/components/ui/` shows all 7 shadcn components
</verification>

<success_criteria>
- Vite project builds successfully with no TypeScript errors
- All 7 shadcn/ui components installed (command, skeleton, sheet, collapsible, card, badge, slider)
- All runtime deps (TanStack Query, React Router, Recharts) and test deps (Vitest, MSW, RTL) installed
- Tailwind v4 configured via @tailwindcss/vite (no postcss.config.js)
</success_criteria>

<output>
After completion, create `.planning/phases/03-react-frontend/03-01-SUMMARY.md`
</output>
